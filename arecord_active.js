/*
 *************************************************
 *              ACTIVE SUBDOMAINS
 *************************************************
 *
 * Currently, the following subdomains are active.
 * To add your own subdomain, please submit a Pull Request.
 *
 * This file uses **A RECORDS** instead of CNAME.
 *
 *************************************************
 *
 * README
 *************************************************
 *
 * KEY:
 *   The subdomain you are requesting.
 *   Example:
 *     "foo" → creates the domain: foo.t50.site
 *
 * VALUE:
 *   The **IP address** where your website is hosted.
 *   This can be:
 *     - GitHub Pages IP
 *     - Netlify IP
 *     - Vercel IP
 *     - Any valid public server IP
 *
 * A RECORD INFO:
 *   An A Record points a domain or subdomain directly
 *   to an IPv4 address instead of another domain name.
 *
 *   Example:
 *     foo.t50.site → 185.199.108.153
 *
 * CLOUDFLARE:
 *   T50.SITE uses Cloudflare for DNS management.
 *   By default:
 *     - Cloudflare proxy is ENABLED
 *     - HTTPS (SSL) is active
 *     - Cache TTL is 30 minutes
 *
 *   If you want Cloudflare proxy OFF (DNS only),
 *   add the comment:
 *     // noCF
 *
 * IMPORTANT:
 *   - The subdomain name should match:
 *       • your GitHub username
 *       • OR your project/repository name
 *   - You must own and control the hosting IP
 *   - Fake, empty, or abusive content is NOT allowed
 *
 * NOTICE:
 *   - Keep all entries in **alphabetical order**
 *   - Use double quotes only
 *   - Add a comma after every line except the last one
 *   - Follow the same format as existing records
 *
 *   ⚠️ Because of abuse risk, requests without
 *   proper and reasonable content will be rejected.
 *
 *************************************************
 */
var arecords_active = {
  "Type_Domain_Name": "type_ip"
  "mobile": "xxxx.xxx.xxx.xx.153",
  "menmylove": "xxxxx.xxx..xxx.x.153",
  "go": "xx.xxx.xx.xx.21"
}
🔍 Difference (Simple Words)

CNAME → domain → another domain

A Record → domain → direct IP address ✅ (more control)
